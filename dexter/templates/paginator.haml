%%def(name="paginator(endpoint, pagination, **kwargs)")
  - kwargs.pop('page', None)
  %ul.pagination
    - for page in pagination.iter_pages():
      - if page:
        - if page != pagination.page:
          %li
            %a(href=url_for(endpoint, page=page, **kwargs))&= page
        - else:
          %li.active
            %a(href=url_for(endpoint, page=page, **kwargs))&= page
      - else:
        %li.disabled
          %a(href="#") ...
